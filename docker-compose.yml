version: '3.8'

services:
  backend:
    build: ./backend
    container_name: vulnview-backend
    environment:
      PORT: 8080
      DB_PATH: /data/vulnview.db
    volumes:
      - ./data:/data
    restart: unless-stopped
    # KEIN ports: - laeuft nur intern im Docker-Netzwerk
