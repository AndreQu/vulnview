
services:
  backend:
    build: ./backend
    container_name: vulnview-backend
    network_mode: host
    environment:
      PORT: 18080
      DB_PATH: ./vulnview.db
    volumes:
      - ./data:/data
    restart: unless-stopped
